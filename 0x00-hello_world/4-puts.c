#include  <stdio.h>

/* main function prints "Programming is like building a multilingual puzzle, followed by a new line and returns 0 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";
        puts(str);
	return (0);
}
